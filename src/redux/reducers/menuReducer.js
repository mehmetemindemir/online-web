import {updateObject} from "../../shared/utility";import * as actionTypes from '../actions/actionTypes'const initialState = {    menuData: null,    error: null,    loading: false,}const menuStart = (state) => {    return updateObject(state, {loading: true, error: null});};const menuSuccess = (state, action) => {    return updateObject(state, {        loading: false,        error: null,        menuData: action.menuData    });};const menuFail = (state, action) => {    return updateObject(state, {        loading: false,        error: action.error    });}const reducer = (state = initialState, action) => {    switch (action.type) {        case actionTypes.MENU_START:            return menuStart(state);        case actionTypes.MENU_SUCCESS:            return menuSuccess(state, action);        case actionTypes.AUTH_FAIL:            return menuFail(state, action);        default:            return state;    }}export default reducer;