import {updateObject} from "../../shared/utility";import * as actionTypes from '../actions/actionTypes'const initialState = {    brandData: null,    error: null,    loading: false}const brandStart = (state) => {    return updateObject(state, {loading: true, error: null});}const brandSuccess = (state, action) => {    return updateObject(state, {        loading: false,        error: null,        brandData: action.brandData    });};const brandFail = (state, action) => {    return updateObject(state, {        loading: false,        error: action.error    });};const reducer = (state = initialState, action) => {    switch (action.type) {        case actionTypes.BRAND_START:            return brandStart(state);        case actionTypes.BRAND_SUCCESS:            return brandSuccess(state, action);        case actionTypes.AUTH_FAIL:            return brandFail(state, action);        default:            return state;    }}export default reducer;